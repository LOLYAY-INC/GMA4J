<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Make java.util.logging (Jersey/Grizzly, bridged via jul-to-slf4j) honour these levels -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>false</withJansi>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan([%thread]) %blue(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress noisy third-party logging (errors still surface) -->
    <logger name="org.glassfish.grizzly" level="ERROR"/>
    <logger name="org.glassfish.jersey" level="ERROR"/>
    <logger name="org.glassfish.tyrus" level="ERROR"/>
    <logger name="org.mongodb" level="ERROR"/>

    <!-- Our code: everything -->
    <logger name="io.lolyay" level="TRACE"/>

    <!-- Root: silence everything else down to warnings -->
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
